export type PluginData = {
    title: string,
    manifest: string
}

export type InstalledPluginData = {
    title: string,
    version: string
}

export type PluginRelease = {
    url: string,
    version: string
}