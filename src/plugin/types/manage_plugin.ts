export type PluginList = {
    title: string,
    manifest: string
}

export type InstalledPluginList = {
    title: string,
    version: string
}

export type PluginRelease = {
    url: string,
    version: string
}